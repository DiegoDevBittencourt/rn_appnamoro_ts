import styled from 'styled-components';

import { GenericContainer } from '@components/index';

export const UserInfoEditorContainer = styled(GenericContainer)`
    height: auto;
    padding-right: 10px;
    padding-left: 10px;
    align-items: center;
`;
