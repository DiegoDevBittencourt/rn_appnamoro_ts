import { ViewStyle } from 'react-native';

export interface AwesomeIconType {
    customIconContainer?: ViewStyle,
    evilIcon?: boolean,
    iconName?: string,
    customIconStyle?: any,
    solidIcon?: boolean
}
